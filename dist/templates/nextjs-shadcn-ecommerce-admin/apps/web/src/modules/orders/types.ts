import type { ListOrdersParams, OrderOutput } from "@/generated/model";

export type { ListOrdersParams };
/** `OrderOutput` aliased as `Order` (module's own conventional name, matching `Product`/`Category`'s aliasing convention). */
export type Order = OrderOutput;
