/**
 * Placeholder root page — Phase 5 (this batch) is infra only, zero page
 * content. `next build` needs at least one route under `app/` to produce a
 * valid App Router build; this is replaced by the real storefront home page
 * in Phase 6 (`app/(ecommerce)/page.tsx` per this template's design tree).
 */
export default function RootPage() {
	return null;
}
