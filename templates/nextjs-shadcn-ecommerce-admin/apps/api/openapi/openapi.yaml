# GENERATED FILE — DO NOT EDIT BY HAND.
#
# Produced by `pnpm generate:openapi` (`scripts/generate-openapi.ts`) from
# this template's Zod schemas via `zod-openapi` — colidevs' code-first
# OpenAPI standard (hefesto ADR 0040, superseding ADR 0005's Apidog-first
# design mandate; see `.claude/rules/api-design-apidog.md`). To change this
# document, edit the Zod schemas (`src/v1/modules/**/types.ts`,
# `src/v1/res/problem-schema.ts`) and/or the route wiring in
# `scripts/generate-openapi.ts`, then re-run the generator.
#
# `pnpm generate:openapi:check` (wired into
# `.github/workflows/api-standard.yml`) fails the build the moment this
# file drifts from a fresh regeneration — ADR 0040's 2026-08-31 addendum.

openapi: 3.1.0
info:
  title: "{{name}}"
  version: 1.0.0
  description: Minimal starter spec for this template's existing endpoints,
    generated from its Zod schemas (ADR 0040). Extend the schema/route modules
    this script imports as the project's real API contract grows — never
    hand-edit this file.
servers:
  - url: /api/v1
paths:
  /healthcheck/status:
    get:
      operationId: getHealthcheckStatus
      summary: Application-level healthcheck status
      security: []
      responses:
        "200":
          description: Healthcheck status message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthcheckStatusResponse"
        default:
          description: Unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /me:
    get:
      operationId: getMe
      summary: Current authenticated caller
      description: Returns the caller's own identity, resolved from the Better Auth
        session (`src/lib/auth.ts` / `src/v1/middlewares/auth.ts`). Requires
        both a valid session cookie AND the service-to-service static key
        (`x-service-key`) — see the `sessionCookie`/`apiKeyAuth` security
        schemes.
      security:
        - sessionCookie: []
          apiKeyAuth: []
      responses:
        "200":
          description: The authenticated caller's identity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          description: Missing or invalid session
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /dlocal/checkout:
    post:
      operationId: createDlocalCheckout
      summary: Create (or replay) a dLocal Go checkout session
      description: Called by this template's own storefront (Server Action), never
        directly by a browser — the `x-service-key` requirement is why.
        Replaying the same `orderId` returns the existing session instead of
        creating a duplicate (idempotent).
      security:
        - apiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckoutRequest"
      responses:
        "200":
          description: The created (or replayed) checkout session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutResponse"
        "409":
          description: Insufficient stock for one or more items
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        "502":
          description: dLocal Go API call failed
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
  /dlocal/notifications:
    post:
      operationId: handleDlocalNotification
      summary: dLocal Go payment-notification webhook
      description: Called by dLocal Go directly, never by this app's own frontend —
        verified via a bespoke HMAC signature check
        (`v1/modules/Dlocal/signature.ts`), not the `apiKeyAuth`/`sessionCookie`
        schemes used elsewhere in this document. Not enforced by
        `express-openapi-validator` at runtime (`v1/modules/Dlocal/route.ts`'s
        `dlocalNotificationRouter` doc comment) — documented here for contract
        completeness only.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DlocalNotification"
      responses:
        "200":
          description: Notification processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                required:
                  - status
        "401":
          description: Invalid or missing webhook signature
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
        default:
          description: Unexpected error
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/Problem"
components:
  schemas:
    CheckoutRequest:
      type: object
      properties:
        orderId:
          type: string
          minLength: 1
          example: order-2026-000123
        payer:
          type: object
          properties:
            name:
              type: string
              example: Jane Doe
            email:
              type: string
              format: email
              pattern: ^(?!\.)(?!.*\.\.)([A-Za-z0-9_'+\-\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\-]*\.)+[A-Za-z]{2,}$
              example: jane@example.com
            document:
              type: string
              example: "12345678"
            phone:
              example: +1 555 0100
              type: string
          required:
            - name
            - email
            - document
        items:
          minItems: 1
          type: array
          items:
            type: object
            properties:
              productId:
                type: string
                format: uuid
                pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
                example: 9c4f3e1a-3b7e-4b1a-9c7a-4d3b6e2f8a1c
              quantity:
                type: integer
                exclusiveMinimum: 0
                maximum: 9007199254740991
                example: 2
            required:
              - productId
              - quantity
      required:
        - orderId
        - items
      description: Creates a dLocal Go checkout session tied to a persisted order.
        Replaying the same orderId returns the existing session instead of
        creating a duplicate.
      example:
        orderId: order-2026-000123
        payer:
          name: Jane Doe
          email: jane@example.com
          document: "12345678"
        items:
          - productId: 9c4f3e1a-3b7e-4b1a-9c7a-4d3b6e2f8a1c
            quantity: 2
    DlocalNotification:
      type: object
      properties:
        payment_id:
          type: string
          example: D-4-e836ba0b-1f9b-4a3e
      required:
        - payment_id
      description: dLocal Go's payment-notification webhook payload.
    HealthcheckStatusResponse:
      type: object
      properties:
        msg:
          type: string
          description: Healthcheck status message
          example: V1 Healthcheck Status OK ✅
      required:
        - msg
      additionalProperties: false
    MeResponse:
      type: object
      properties:
        id:
          type: string
          example: usr_01hz8p7q9k2m3n4p5q6r7s8t9u
        email:
          type: string
          format: email
          pattern: ^(?!\.)(?!.*\.\.)([A-Za-z0-9_'+\-\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\-]*\.)+[A-Za-z]{2,}$
          example: jane@example.com
      required:
        - id
        - email
      additionalProperties: false
    CheckoutResponse:
      type: object
      properties:
        orderId:
          type: string
          example: order-2026-000123
        dlocalId:
          type: string
          example: D-4-e836ba0b-1f9b-4a3e
        status:
          type: string
          example: PENDING
        redirectUrl:
          type: string
          format: uri
          example: https://checkout.dlocalgo.com/pay/D-4-e836ba0b
      required:
        - orderId
        - dlocalId
        - status
        - redirectUrl
      additionalProperties: false
      description: The created (or replayed) dLocal checkout session, including the
        redirect URL the storefront sends the buyer to.
    Problem:
      type: object
      properties:
        type:
          type: string
          format: uri-reference
          description: A URI reference identifying the problem type.
          example: https://coli.dev/errors/not-found
        status:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
          example: 404
        title:
          type: string
          example: Resource not found.
        detail:
          example: Resource not found. order 1
          type: string
        instance:
          example: /api/v1/orders/1
          type: string
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
                example: email
              message:
                type: string
                example: must be a valid email
            required:
              - field
              - message
            additionalProperties: false
      required:
        - type
        - status
        - title
      additionalProperties: false
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: better-auth.session_token
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    apiKeyAuth:
      type: apiKey
      in: header
      name: x-service-key
