node_modules
temp
.DS_Store
.npmrc
# Exception: a template's own .npmrc (e.g. scoped @colidevs registry mapping
# for @colidevs/utils, kumo-console-template Phase 3) carries no secrets and
# must ship with every project scaffolded from that template.
!templates/*/.npmrc